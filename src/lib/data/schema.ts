export interface Country {
    id: number;
    code: string;
    name: string;
}

export interface Region {
    code: number;
    name: string;
}

export interface Subregion {
    code: number;
    name: string;
}